[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -e ."]

[phases.build]
cmds = ["echo 'no build step needed'"]

[start]
cmd = "uvicorn gyros_os.server.main:app --host 0.0.0.0 --port $PORT"
