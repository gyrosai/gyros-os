[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install --upgrade pip",
  ". /opt/venv/bin/activate && pip install -e ."
]

[start]
cmd = "/opt/venv/bin/uvicorn gyros_os.server.main:app --host 0.0.0.0 --port 8080"
